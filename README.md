# Mile-Marker
It's one self-contained file with zero subscriptions, zero servers, and zero dependence on Claude: everything is computed on your device with real sports-science formulas, so it works forever.
